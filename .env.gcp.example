# CASA - GCP Cloud Run Environment Variables
# Copy this file to .env.production and update values
#
# FedRAMP Moderate Compliance:
# - All services run in FedRAMP-authorized region (us-east4)
# - Secrets stored in Secret Manager (never in code)
# - Cloud Audit Logs enabled for all API calls
# - Data encrypted at rest with Google-managed or CMEK keys
# - VPC Service Controls recommended for production

# ===========================================
# FRONTEND (Next.js) Environment Variables
# ===========================================

# WordPress Backend URL (Cloud Run service URL)
NEXT_PUBLIC_WORDPRESS_URL=https://casa-backend-xxxxx-ue.a.run.app

# WordPress API URL for server-side requests
WORDPRESS_API_URL=https://casa-backend-xxxxx-ue.a.run.app

# JWT Configuration
JWT_SECRET=your-secure-jwt-secret-generate-with-openssl-rand-base64-32
NEXTAUTH_SECRET=your-nextauth-secret-generate-with-openssl-rand-base64-32
NEXTAUTH_URL=https://casa-frontend-xxxxx-ue.a.run.app

# Environment
NODE_ENV=production

# ===========================================
# BACKEND (WordPress) Environment Variables
# ===========================================

# Database Configuration (Cloud SQL)
WORDPRESS_DB_HOST=/cloudsql/gov-webhosting-east-1766330024:us-east4:wordpress-db
WORDPRESS_DB_NAME=wordpress
WORDPRESS_DB_USER=wordpress
WORDPRESS_DB_PASSWORD=stored-in-secret-manager

# WordPress Site URLs
WORDPRESS_SITE_URL=https://casa-backend-xxxxx-ue.a.run.app
WORDPRESS_HOME_URL=https://casa-backend-xxxxx-ue.a.run.app

# JWT Secret Key (same as frontend)
JWT_SECRET_KEY=your-secure-jwt-secret-generate-with-openssl-rand-base64-32

# GCS Bucket for Media Uploads
GCS_BUCKET_NAME=casa-media-1766330672

# Frontend URL for CORS
FRONTEND_URL=https://casa-frontend-xxxxx-ue.a.run.app

# WordPress Auth Keys (generate at https://api.wordpress.org/secret-key/1.1/salt/)
WORDPRESS_AUTH_KEY=generate-unique-key
WORDPRESS_SECURE_AUTH_KEY=generate-unique-key
WORDPRESS_LOGGED_IN_KEY=generate-unique-key
WORDPRESS_NONCE_KEY=generate-unique-key
WORDPRESS_AUTH_SALT=generate-unique-salt
WORDPRESS_SECURE_AUTH_SALT=generate-unique-salt
WORDPRESS_LOGGED_IN_SALT=generate-unique-salt
WORDPRESS_NONCE_SALT=generate-unique-salt

# Debugging (set to false in production)
WP_DEBUG=false
